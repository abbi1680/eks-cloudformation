apiVersion: v1
kind: Service
metadata:
    namespace: staging
    labels:
        run: frontend-service
    name: frontend-service
spec:
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 80
  selector:
    app: frontend
  type: LoadBalancer